'use estrict';
const priceKiwi = 5;
const pricePear = 2;
const priceGrape = 4;

const toPay = 2 * parseInt(priceKiwi) + 3 * parseInt (pricePear) + 0.5 * parseInt(priceGrape);

console.log ('Total a pagar ${toPay} €');
